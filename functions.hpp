#ifndef H_FUNCTIONS
#define H_FUNCTIONS

//function projects
//defined in "functions.cpp"

bool isEven(int& a); //function checks whether a value is even or not

void swap(int& b, int& c); //function swaps values of @param b and @param c

bool leapYear(int& currentYear); //function checks whether the given year is a leap year

void calc(float& calc_a, float& calc_b); //basic calculator function

int ABS(int& d); //function returns ABS of @param d

#endif